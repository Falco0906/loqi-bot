[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
    "integration: requires a provisioned integration environment or external system",
    "requires_db: requires a live database with the current migration set",
]
